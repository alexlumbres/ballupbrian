# deps / build
node_modules/
.next/
out/
dist/

# scratch & local (not part of the site)
.backups/
_anim/
_source-video/
images/shutter_sprite.png

# cloudflare local state (holds account cache — keep out of the remote)
.wrangler/

# agent tooling
.agents/
.codex/
skills-lock.json

# os / logs
*.log
.DS_Store
.env*.local
